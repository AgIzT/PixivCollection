name: Build

on:
  push:
    branches:
      - main
  workflow_dispatch:
  # 每天北京时间早上 4 点自动运行
  schedule:
    - cron: '0 20 * * *'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Enable corepack
        run: corepack enable

      - name: Setup Node.js environment
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'pnpm'

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Run Pixiv Crawler
        env:
          REFRESH_TOKEN: ${{ secrets.REFRESH_TOKEN }}
          R2_BUCKET_NAME: ${{ secrets.R2_BUCKET_NAME }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
        run: |
          git clone https://github.com/orilights/python_scripts.git
          cd python_scripts/pixiv_collection
          pip install -r requirements.txt
          
          # === 🔧 配置修改区 ===
          sed -i 's/from .collection/from collection/g' example.py
          sed -i 's/USER_ID = .*/USER_ID = 33443193/' example.py
          sed -i "s/REFRESH_TOKEN = .*/import os; REFRESH_TOKEN = os.environ.get('REFRESH_TOKEN')/" example.py
          # 修复第一次运行报错
          sed -i "s/c.read_data('collection.json')/if os.path.exists('collection.json'): c.read_data('collection.json')/" example.py
          
          # 👇【关键修改】只抓取 1 页 (安全防风控)
          # 原脚本默认是 n_pages=2，我们把它改成 1
          sed -i 's/n_pages=2/n_pages=1000/g' example.py
          
          echo "🚀 开始安全抓取 (仅抓取第1页)..."
          python example.py
          
          # === 📂 搬运数据 ===
          mkdir -p ../../public
          
          # 简化版搬运逻辑
          [ -f "collection.json" ] && mv collection.json ../../public/images.json || echo "[]" > ../../public/images.json
          
          echo "✅ 处理完成！"

      # --- ✨ 提交数据回仓库 ---
      - name: Commit files
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add public/images.json
          git commit -m "Auto update images.json [skip ci]" || echo "No changes to commit"
          git push
# ... (前面的步骤不用动)

      # 👇 新增这个步骤，专门用来找文件夹！
      - name: Check file structure
        run: |
          echo "==== 根目录文件 ===="
          ls -F
          echo "==== Public 目录文件 ===="
          ls -F public/ || echo "Public 目录不存在"
          echo "==== 寻找 image 文件夹 ===="
          find . -type d -name "image" -o -name "images"

      - name: Upload to R2
        uses: ryand56/r2-upload-action@latest
        with:
          r2-account-id: ${{ secrets.R2_ACCOUNT_ID }}
          r2-access-key-id: ${{ secrets.R2_ACCESS_KEY_ID }}
          r2-secret-access-key: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          r2-bucket: pixiv-backup
          # 👇 这里填我们刚找到的“真实地址”
          source-dir: python_scripts/pixiv_collection/image
          # 👇 目标地址保持不变，这样你的 R2 桶里就会整整齐齐显示 image/original 等文件夹
          destination-dir: image
# 👇 这是新增的步骤：专门把 images.json 传到 R2 根目录
      - name: Upload Menu (JSON) to R2
        uses: ryand56/r2-upload-action@latest
        with:
          r2-account-id: ${{ secrets.R2_ACCOUNT_ID }}
          r2-access-key-id: ${{ secrets.R2_ACCESS_KEY_ID }}
          r2-secret-access-key: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          r2-bucket: pixiv-backup
          source-dir: public
          destination-dir: .
