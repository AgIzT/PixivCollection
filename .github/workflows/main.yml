name: Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Enable corepack
        run: corepack enable

      - name: Setup Node.js environment
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'pnpm'

      # --- 🐍 Python 爬虫部分 ---
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Run Pixiv Crawler
        env:
          REFRESH_TOKEN: ${{ secrets.REFRESH_TOKEN }}
          R2_BUCKET_NAME: ${{ secrets.R2_BUCKET_NAME }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
        run: |
          git clone https://github.com/orilights/python_scripts.git
          cd python_scripts/pixiv_collection
          pip install -r requirements.txt
          
          # ================= 🔧 终极修复代码 =================
          
          # 1. 修复导入路径
          sed -i 's/from .collection/from collection/g' example.py
          
          # 2. 填入 UID (33766904)
          sed -i 's/USER_ID = .*/USER_ID = 33766904/' example.py
          
          # 3. 让脚本直接从环境变量读取 Token (防错)
          sed -i "s/REFRESH_TOKEN = .*/import os; REFRESH_TOKEN = os.environ.get('REFRESH_TOKEN')/" example.py

          # 4. 【新增修复】 解决“第一次运行找不到文件”的报错
          # 意思是：如果 collection.json 存在才读取，不存在就跳过
          sed -i "s/c.read_data('collection.json')/if os.path.exists('collection.json'): c.read_data('collection.json')/" example.py
          
          # ================= 🔧 修复结束 =================
          
          echo "🚀 开始运行爬虫..."
          python example.py
          
          if [ -f "collection.json" ]; then
            mv collection.json ../../images.json
            echo "✅ 数据生成成功！"
          else
            # 如果是第一次运行，可能脚本还没生成 json 就结束了（或者图太少）
            # 我们手动创建一个空的 images.json 防止后面报错
            echo "[]" > ../../images.json
            echo "⚠️ 初次运行，手动生成了基础数据文件。"
          fi

      - name: Build
        run: pnpm install && pnpm build
